interface Member {
  bookId?: string; 
  title?: string; 
  status?: string;
  borrowedDate?: string;
  dueDate?: string;
  memberId: string;
  memberName: string;
  totalDue?: number;
  paymentStatus?: string;
  phoneNumber: string;   
  address: string;       
}
